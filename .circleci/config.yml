version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - python/load-cache
      - run:
        command: echo 123456789
      - python/install-deps
      - python/save-cache

workflows:
  main:
    jobs:
      - build-and-test