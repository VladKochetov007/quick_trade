version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - python/load-cache
      - run:
          command: ls
          name: install deps
      - python/install-deps
      - python/save-cache

workflows:
  main:
    jobs:
      - build-and-test